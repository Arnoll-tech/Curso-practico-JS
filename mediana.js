const lista1 = [
    100,
    200, 
    500, 
    600,
    3000, 
    
];

const mitadLista1 = parseInt(lista1.length / 2);
function esPar(numerito) {
    if (numerito % 2 === 0) {
        
    } else {
        
    }
    return false
}

let mediana = 0;

if (lista1 [lista1.length % 2 === 0]) {
    
} else {
    mediana = lista1[mitadLista1];
    
}